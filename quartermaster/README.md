# Quartermaster

Quartermaster is a self-hosted kitchen inventory app for households.

This Home Assistant App runs the official Quartermaster container image from GitHub Container Registry and stores its SQLite database in the App data volume.
